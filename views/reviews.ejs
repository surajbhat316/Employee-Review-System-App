<link rel="stylesheet" href="/css/reviews.css">
<link rel="stylesheet" href="/css/media/media_reviews.css">
<div class="body-container">
    <h2>Your Reviews</h2>
    <%if(reviewsAsAUser.length == 0){%>
        <p>No Reviews found </p>
    <%} else {%>
        <div class="review-main-container">
            <%for(let review of reviewsAsAUser){%>
                <div class="review-container">
                    <div class="review-container-info">
                        <p><b>Title</b> : <%=review.title%></p>
                        <p><b>Reviewed User</b> : <%= review.reviewed_user.name%></p>
                        <p><b>Created By</b> : <%=review.created_by_user.name%></p>
                    </div>
                    <a href="/reviews/detail/<%=review._id%>">View Details</a>
                </div>
                
            <%}%>
        </div>
    <%}%> 


    <h2>Reviews requiring your feedback </h2>
    <%if(reviewAsAnAssociatedUser.length == 0){%>
        <p>No Reviews found </p>
    <%} else {%>
        <div class="review-main-container">
            <%for(let review of reviewAsAnAssociatedUser){%>
                <div class="review-container">
                    <div class="review-container-info">
                        <p><b>Title</b> : <%=review.title%></p>
                        <p><b>Reviewed User</b> : <%= review.reviewed_user.name%></p>
                        <p><b>Created By</b> : <%=review.created_by_user.name%></p>
                    </div>
                    <a href="/reviews/detail/<%=review._id%>">View Details</a>
                </div>
                
            <%}%>
        </div>

    <%}%> 


    <%if(locals.user.isAdmin){%>
        <h2>Reviews Created By You</h2>
        <%if(reviewAsAnAdminUser.length == 0){%>
            <p>No Reviews found </p>
        <%} else {%>
            <div class="review-main-container">
                <%for(let review of reviewAsAnAdminUser){%>
                    <div class="review-container">
                        <div class="review-container-info">
                            <p><b>Title</b> : <%=review.title%></p>
                            <p><b>Reviewed User</b> : <%= review.reviewed_user.name%></p>
                            <p><b>Created By</b> : <%=review.created_by_user.name%></p>
                        </div>
                        <a href="/reviews/detail/<%=review._id%>">View Details</a>
                    </div>
                    
                <%}%>
            </div>
        <%}%>  
    <%}%>
</div>


